#include <stdio.h>
#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int num = 40;
int *pNum = &Num;

void func (int *pNum);

*pNum = 98;

int main () 
{
	putchar (Num);
	
	return (0);
}
